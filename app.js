angular.module('app'), []);
/* angular.module() function acts as a setter and getter for angular modules
the first argument ('app') is to show that we're going to be making an application
and the [] tags indicate array. */
